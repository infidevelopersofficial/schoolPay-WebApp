# `.env.example` — Copy to `.env` and fill in secrets locally
# Do NOT commit your `.env` file. Keep secrets out of source control.

# App
NODE_ENV=development
PORT=3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database (example for Postgres)
# Example: postgresql://user:password@localhost:5432/schoolpay
DATABASE_URL=

# Next.js / Authentication
NEXTAUTH_URL=http://localhost:3000
# Generate a strong secret for NextAuth / session signing
NEXTAUTH_SECRET=

# Server Actions / encryption key (optional for Next.js server actions)
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=

# Third-party services (placeholders)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# Other optional public vars (prefix with NEXT_PUBLIC to expose to client)
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_MAPS_API_KEY=

# Example: set to "production" in your production environment
# NODE_ENV=production
